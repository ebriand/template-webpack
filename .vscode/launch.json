{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attacher",
            "type": "node",
            "request": "attach",
            "port": 5858,
            "address": "localhost",
            "restart": false,
            "sourceMaps": true,
            "outDir": "${workspaceRoot}/dist/server",
            "localRoot": "${workspaceRoot}",
            "remoteRoot": null
        }

    ]
}